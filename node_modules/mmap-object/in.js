#!/usr/bin/env node
'use strict'
const MmapObject = require('.')

const obj = {
  one: 1,
  two: 2,
  three: 'a string',
  four: 'also a string'
}

//const buf = Buffer.from('©´®∫ˆ¬')
const buf = Buffer.from([65, 66, 0, 66])

const so = new MmapObject.Create('/tmp/obj.bin', 1000, 100)
so.in = buf
console.log(typeof buf)
console.log(buf)
so.close()

const so2 = new MmapObject.Open('/tmp/obj.bin')
console.log(so2.in)
console.log(typeof so2.in)
so2.close()
