import {BaseTree} from './BaseTree';

export interface ITreeData<T> {
    id: string | number;
    children?: Array<ITreeData<T>>;
    data?: T;
}

export interface ITreeOptions {
    Child?: typeof BaseTree;
}
