import { BasePart } from './BasePart';
import { IArrayPart } from './interfaces';
export declare class ArrayPart extends BasePart<IArrayPart> {
    private _child;
    constructor(config: IArrayPart, path?: string);
    process(data: any, rootList: Array<any>): Promise<any>;
    protected getValue(data: any): Array<any>;
}
