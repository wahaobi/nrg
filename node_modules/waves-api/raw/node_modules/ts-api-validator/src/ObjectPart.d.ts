import { BasePart } from './BasePart';
import { IObjectPart } from './interfaces';
export declare class ObjectPart extends BasePart<IObjectPart> {
    private _childHash;
    constructor(config: IObjectPart, path?: string);
    process(data: any, rootList: Array<any>): Promise<any>;
    protected getValue(data: any): Array<any>;
}
